# 实现计划 · 阶段 0 / 1 / 2

关联：[需求文档.md](./需求文档.md) · [技术选型与目录约定.md](./技术选型与目录约定.md) · [实现计划-阶段345.md](./实现计划-阶段345.md)

## 阶段 0（已锁定）

见需求文档第 6 节「已确认」规则。

## 阶段 1：鉴权

- 用户注册（昵称+密码+头像）/ 登录 / me
- 管理员 `.env` 账号 + Session 管理站登录；`POST /api/admin/login` 发 admin JWT
- App Token 持久化

### 验收

- [x] `/docs` 注册 → 登录 → me；重复昵称失败
- [x] 管理站可登录（`/admin/login`）
- [x] App 注册/登录/Token（已实现并打出 debug APK）

## 阶段 2：漫画闭环

- Admin：pages CRUD（API + 管理站）
- User：`GET /api/pages`、`GET /api/pages/{id}`（需登录）
- App：下拉刷新列表 + 阅读大图

### 验收

- [x] API：后台上传 → 用户列表/详情可见（`scripts/smoke_phase012.py`）
- [x] 无 Token 访问 pages 返回 401
- [x] 局域网健康检查 `http://192.168.1.120:8000/api/health` 可达
- [x] Debug APK 已打包：`app/reader/build/outputs/apk/debug/reader-debug.apk`
- [ ] 真机安装后目视确认下拉与大图（需手机安装 APK）

## 范围外

评论/点赞/消息、删用户、推送、多章节。
